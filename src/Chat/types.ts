export type ChatMessage = {
  messageId: string,
  userId: string,
  message: string,
  sentAt: string
};

export type ChatMessageResponse = {
  message_id: string,
  user_id: string,
  message: string,
  sent_at: string,
};

export type ChatMessageRequest = {
  
};
